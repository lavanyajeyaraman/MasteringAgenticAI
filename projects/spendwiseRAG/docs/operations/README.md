# Operations

Use this folder for deployment runbooks, production checklists, and incident notes.

Current local commands:

```bash
make run
make test
```

Docker entrypoint:

```bash
docker compose -f deploy/docker/docker-compose.yml up --build
```
